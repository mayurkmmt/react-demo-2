import { createContext } from 'react';

/**
 * this is context, which i have used for store data, which i can use in child component of dashboard
 */
const ChartContext = createContext();
export default ChartContext;